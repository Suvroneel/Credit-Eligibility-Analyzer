# Infra

Contains Serverless Framework configuration files and DB schema.

## Files
- `serverless.yml` — deploy Lambdas, S3 bucket, IAM roles
- `rds_schema.sql` — SQL schema for Postgres tables
- `README.md` — this file

## Notes
- Update `.env` with your credentials before deploying
- You can create RDS manually (simpler for free-tier)
